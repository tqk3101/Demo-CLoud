<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Toy Shop</title>
   <!-- Bootstrap CSS -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
   <style>
      body {
         background-color: #f0f4f8; /* Soft light gray background */
         color: #333; /* Dark text color for readability */
      }

      .container {
         background-color: #ffffff; /* White background for the content area */
         border-radius: 10px;
         box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
         padding: 30px;
         margin-top: 20px;
      }

      .text-danger {
         color: #d9534f; /* Red color for the heading */
      }

      img {
         max-width: 100%;
         height: auto;
         border-radius: 10px; /* Rounded corners for the image */
         margin-top: 20px;
         margin-bottom: 20px;
      }

      .btn-manager {
         background-color: #28a745; /* Green background */
         border-color: #28a745;
         color: white;
      }

      .btn-manager:hover {
         background-color: #218838; /* Darker green on hover */
         border-color: #1e7e34;
      }

      .btn-customer {
         background-color: #17a2b8; /* Teal background */
         border-color: #17a2b8;
         color: white;
      }

      .btn-customer:hover {
         background-color: #138496; /* Darker teal on hover */
         border-color: #117a8b;
      }
   </style>
</head>

<body>
   <div class="container text-center">
      <h1 class="text text-danger mt-3 mb-3">{{ title }}</h1>
      <img src="/images/TOYSHOP.jpg" alt="Toy Shop">

      <div class="d-flex justify-content-center">
         <a href="/toy" class="btn btn-manager m-2">ADMIN</a>
         <a href="/view" class="btn btn-customer m-2">Customer</a>
      </div>
   </div>

   <!-- Bootstrap JS (Optional) -->
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
