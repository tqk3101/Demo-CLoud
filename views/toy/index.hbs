<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Toy List</title>
   <!-- Bootstrap CSS -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
   <!-- Bootstrap Icons -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
   <style>
      body {
         background-color: #eaeaea; /* Soft gray background */
         color: #333; /* Dark text color */
      }

      .card-custom {
         max-width: 1000px;
         margin: auto;
         border: none;
         border-radius: 15px;
         box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
      }

      .card-header-custom {
         background-color: #3f51b5; /* Deep indigo header */
         color: white;
         border-top-left-radius: 15px;
         border-top-right-radius: 15px;
         text-align: center;
         padding: 20px;
         font-size: 28px;
         font-weight: bold;
      }

      .table-responsive {
         padding: 20px;
      }

      .table {
         background-color: white;
         border-radius: 10px;
         overflow: hidden;
         box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
      }

      .table th,
      .table td {
         vertical-align: middle;
         text-align: center;
         padding: 15px;
      }

      .table th {
         background-color: #f0f4f8; /* Light gray background for headers */
         color: #3f51b5; /* Matching header color */
      }

      .table tbody tr:hover {
         background-color: #c5cae9; /* Light indigo on hover */
      }

      .btn-custom {
         border-radius: 50px;
         padding: 8px 16px;
         font-size: 14px;
         font-weight: bold;
      }

      .btn-info {
         background-color: #009688; /* Teal background */
         border-color: #009688;
         color: white;
      }

      .btn-info:hover {
         background-color: #00796b; /* Darker teal on hover */
         border-color: #00796b;
      }

      .btn-warning {
         background-color: #ffb300; /* Amber background */
         border-color: #ffb300;
         color: white;
      }

      .btn-warning:hover {
         background-color: #ffa000; /* Darker amber on hover */
         border-color: #ffa000;
      }

      .btn-danger {
         background-color: #f44336; /* Red background */
         border-color: #f44336;
         color: white;
      }

      .btn-danger:hover {
         background-color: #c62828; /* Darker red on hover */
         border-color: #c62828;
      }

      @media (max-width: 576px) {
         .table img {
            width: 80px;
            height: 100px;
         }
      }
   </style>
</head>

<body>
   <div class="container my-5">
      <div class="card card-custom">
         <div class="card-header card-header-custom">
            PRODUCTS LIST
         </div>
         <div class="table-responsive">
            <div class="mb-3 text-end">
               <a href="/toy/add" class="btn btn-success btn-custom">Add New Toy</a>
            </div>
            <table class="table table-hover table-bordered">
               <thead>
                  <tr>
                     <th>Name</th>
                     <th>Price</th>
                     <th>Cover</th>
                     <th>Menu</th>
                  </tr>
               </thead>
               <tbody>
                  {{#each toys }}
                  <tr>
                     <td>{{ title }}</td>
                     <td>${{ price }}</td>
                     <td>
                        <a href="/toy/detail/{{ _id}}">
                           <img src="{{ cover }}" alt="toy cover" class="img-fluid rounded"
                              style="width: 100px; height: 120px;">
                        </a>
                     </td>
                     <td>
                        <a class="btn btn-info btn-sm mb-1" href="/toy/detail/{{ _id}}">
                           <i class="bi bi-info-circle"></i>
                        </a>
                        <a class="btn btn-warning btn-sm mb-1" href="/toy/edit/{{ _id}}">
                           <i class="bi bi-pencil-square"></i>
                        </a>
                        <a class="btn btn-danger btn-sm" href="/toy/delete/{{ _id}}"
                           onclick="return confirm('Are you sure to delete this toy?')">
                           <i class="bi bi-trash"></i>
                        </a>
                     </td>
                  </tr>
                  {{/each}}
               </tbody>
            </table>
         </div>
      </div>
   </div>

   <!-- Bootstrap JS (Optional) -->
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
